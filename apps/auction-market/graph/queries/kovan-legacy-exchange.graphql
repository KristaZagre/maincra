query Pairs {
  KOVAN_EXCHANGE_pairs(first: 1000) {
    id
    token0 {
      id
      symbol
      name
      decimals
    }
    token1 {
      id
      symbol
      name
      decimals
    }
  }
}
query Tokens {
  KOVAN_EXCHANGE_tokens(first: 1000) {
    id
    symbol
    name
    decimals
  }
}